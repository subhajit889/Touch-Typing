import React from 'react';
import ReactDOM from 'react-dom/client';
import TypingDetectorApp from './TypingDetectorApp';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TypingDetectorApp/>
  </React.StrictMode>
);

